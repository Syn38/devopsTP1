Pour annuler un commit push sur le serveur,
On revient au commit précédent:
	git reset HEAD~1
Puis on commit/push: (sans add pour ne pas considérer les modifications locales)
	git commit -m "CANCEL"
	git push https://github.com/Syn38/devopsTP1.git master
